package hellojpa;import javax.persistence.Column;import javax.persistence.Entity;import javax.persistence.Id;import javax.persistence.Table;@Entity//@Table(name = "member")//매핑하려는 클래스명과 사용할 테이블 명이 다른경우 @Table어노테이션으로 매핑할 테이블의 이름을 명시해주면됨// 지금은 테이블명과 객체명이 동일하기때문에 어노테이션이 없더라도 알아서 매핑해줌public class Member {    @Id //jpa에게 pk를 알려줌    private Long id;    //@Column(name = "name")    //마찬가지로 컬럼명과 변수명이 다른경우에도 매핑하려는 컬럼을 명시해주면 됨    private String name;    public Member(){}    public Member(Long id, String name){        this.id = id;        this.name = name;    }    public Long getId() {        return id;    }    public void setId(Long id) {        this.id = id;    }    public String getName() {        return name;    }    public void setName(String name) {        this.name = name;    }}