package section5;import javax.persistence.*;import java.util.ArrayList;import java.util.List;@Entitypublic class Team5 {    @Id @GeneratedValue    @Column(name = "TEAM_ID")    private Long id;    private String name;    @OneToMany(mappedBy = "team") //mappedBy에는 연관되어있는 "변수명"을 입력해주면됨(컬럼명X)    private List<Member5> member = new ArrayList<>();    public Long getId() {        return id;    }    public void setId(Long id) {        this.id = id;    }    public String getName() {        return name;    }    public void setName(String name) {        this.name = name;    }    public List<Member5> getMember() {        return member;    }    public void setMember(List<Member5> member) {        this.member = member;    }//new ArrayList<>() : 관례적으로 사용, ArrayList로 초기화해서 add할때 null point가 안 뜨게끔}